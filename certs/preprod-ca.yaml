apiVersion: rkeprep/v1
kind: ClusterCA
metadata:
  name: preprod-ca
spec:
  # paths may be absolute or relative to the script directory
  rootCrt: /etc/rancher/rke2/stage/certs/dev-certa001-ca-dev-local.crt
  rootKey: /etc/ranger/rke2/stage/certs/dev-certa001-ca-dev-local-key.pem
  #intermediateCrt: 
  #intermediateKey: 
  installToOSTrust: true
