# Action Report Notes

This directory contains implementation reports and behavior snapshots for specific actions.

Current-state clarifications:
- Operational source of truth is ../../README.md and ../README.md.
- Current script path is ./bin/rke2nodeinit.sh.
- Boot service behavior is ISO-based and reads YAML from /config on attached ISO media.

If a report snippet conflicts with current docs, follow the current docs and treat the report snippet as historical analysis context.
